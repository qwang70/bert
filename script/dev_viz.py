import numpy as np
import seaborn as sns
import matplotlib.pyplot as plt

sns.set(style="white", palette="muted", color_codes=True)

iterrogative_word = ['which', 'what', 'whose', 'who', 'whom', 'where', 'when', 'how', 'why', 'is']
